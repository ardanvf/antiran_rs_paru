<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mazenrashed.example">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_rspjember"
        android:label="Antrian Rs Paru"
        android:roundIcon="@drawable/logo_rspjember"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="com.mazenrashed.example.ApplicationClass">
        <activity
            android:name="com.mazenrashed.example.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".WoosimActivity" />
    </application>

</manifest>